# --- Base de datos PostgreSQL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/taller_mecanico
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Que spring cree las tablas automáticamente ---
spring.jpa.hibernate.ddl-auto=update

# --- Dialecto ---
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Mostrar SQL en consola ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Forzar que data.sql se ejecute si las tablas están vacías ---
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# --- Puerto ---
server.port=8080

properties
# --- CORS (Configuración para permitir todas las conexiones) ---
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*
# La siguiente línea se elimina porque entra en conflicto con allowed-origins=*
# spring.web.cors.allow-credentials=true 
